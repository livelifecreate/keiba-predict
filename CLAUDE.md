# 競馬予想システム プロジェクトメモ

## 実行コマンド

```bash
python3 saturday_predict.py          # 土曜日の全レース予想（2勝クラス以上）
python3 sunday_predict.py            # 日曜日の全レース予想（2勝クラス以上）
python3 weekend_predict.py --sat     # 土曜（直接実行）
python3 weekend_predict.py --sun     # 日曜（直接実行）
python3 verify/bet_metrics_standard.py              # 全期間ROI検証（9馬券種×クラス別）
python3 verify/bet_metrics_standard.py --surface 芝 # 芝のみ絞り込み
```

## トラブルシューティング

### JRAオッズ取得失敗時

予想実行中に以下が表示された場合:
```
⚠ [JRA] パラメータエラー検出 — チェックサム式が変更された可能性があります
        今すぐ修正: python3 tools/jra_checksum_diag.py --fix
```

**対処手順:**
1. `python3 tools/jra_checksum_diag.py` を実行して診断
2. 不一致が出たら `python3 tools/jra_checksum_diag.py --fix` で自動修正
3. 修正後に `python3 saturday_predict.py` を再実行

**注意:** JRAオッズは発走前のみ取得可能。発走後の `[オッズ] 取得失敗` は正常動作。
パラメータエラーはチェックサム式変更が原因のため、上記コマンドで解決できる。

## 主要ファイル

| ファイル | 役割 |
|---|---|
| `weekend_predict.py` | 予想メイン（sat/sun共通ロジック） |
| `saturday_predict.py` / `sunday_predict.py` | 曜日別エントリポイント |
| `scorer_turf.py` | 芝採点ロジック |
| `scorer_dart.py` | ダート採点ロジック |
| `jra_scraper.py` | JRA公式出馬表取得・チェックサム計算 |
| `netkeiba_scraper.py` | netkeiba スクレイピング |
| `verify/bet_metrics_standard.py` | 標準ROI検証（9馬券種） |
| `cache/race_result/` | 過去レース結果JSON（バックテスト用） |
| `cache/payouts/` | 払戻データ（三連複・馬連等） |
| `data/検証_新ロジック_調教あり.csv` | 現行採点済みCSV（バックテスト入力） |
| `MEETINGS.md` | 会議議事録アーカイブ |

---

## 機能導入前検証ルール（2026-06-14制定）

**新機能・新ロジックは以下を満たさなければ本番実装しない：**
- 検証サンプル件数を明記すること（最低ライン：小幅修正20件以上、新軸追加50件以上）
- 期待値（回収率）が100%を超えることをバックテストで示すこと
- 既存ロジックとの重複・干渉がないことを確認すること

---

## バックテスト基準値（2026-08-23更新・データ欠損穴埋め後・n=1660）

**対象: 2024/06〜2026/08・2勝クラス以上・芝+ダ合計**
**現行ロジック: 2年縛り + jockey_form + training_form + class_achievement + 三連複1軸-相手 実装済み**
**重要: 2024/10〜2025/05と2025/09の欠損を穴埋め（fetch_period.py）し、全期間連続データで再算出。旧n=582の値は虫食いデータで歪んでいたため下表に置き換え。**

### 馬券種別ROI（全期間 n=1660レース）

| 馬券種 | 点数/R | 的中率 | 全体ROI | 2勝 | 3勝 | OP | 重賞 |
|---|---|---|---|---|---|---|---|
| 単勝 | 1 | 22.3% | 102.5% | 83.9% | 139.9% | 100.1% | 97.6% |
| 複勝 | 1 | 44.9% | 90.9% | 80.6% | 98.4% | 81.9% | 118.6% |
| 馬連1-2 | 1 | 8.1% | 120.3% | 60.4% | **185.7%** | **200.1%** | 100.3% |
| ワイド1-2 | 1 | 16.1% | 118.2% | 108.9% | 113.8% | 146.3% | 122.1% |
| ワイド3点 | 3 | 31.7% | 111.3% | 105.8% | 88.8% | 154.6% | 117.1% |
| 三連複1軸4頭 | 6 | 11.0% | 107.2% | 98.8% | 103.2% | **161.1%** | 79.6% |
| 三連複5頭BOX | 10 | 16.2% | 104.3% | 81.5% | 95.3% | **195.0%** | 87.2% |

- データ穴埋めで、旧n=582で赤字だった三連複5頭BOX(79.9%→104.3%)・1軸4頭(78.6%→107.2%)・ワイド3点(90%→111%)が黒字化。旧基準は虫食い（特に2024夏の高配当偏り）で歪んでいた。
- OPは全馬券最強（馬連200%・三連複5box195%・1軸4頭161%）。3勝も馬連186%・単勝140%で堅い。2勝は依然弱く見送り妥当。
- **8/22単日の全滅や「2026年軟調」は小サンプル下振れ**。全期間フルデータでは現行ロジックは健全な黒字。

### 人気軸ハイブリッド（1番人気+予想相手）＝保留（2026-08-23検証）

「軸=1番人気・相手=予想上位」の三連複を検証。D案(1番人気+予想1-4位・約5.5点)は全期間ROI夏123%/非夏116%で、相手を市場人気順にした場合(72%)を大きく上回り**予想が相手選定で価値を出すことは実証**。だが月別ROIが15〜408%と極端な高分散（少数高配当依存）で、2026年は各月40〜56%と軟調。**分散が実運用にきつく本採用は保留**。構造的価値は記録として残す。

<!-- 旧基準(2026-07-12・n=582)は虫食いデータのため撤去。推移はgit log参照 -->

## （旧）バックテスト基準値・判定結果（2026-07-12・n=582・虫食いデータ・参考）

**対象: 2025/12〜2026/06・2勝クラス以上・芝+ダ合計**
**現行ロジック: 2年縛り + jockey_form + training_form 実装済み（scorer_turf.py / scorer_dart.py）**
**旧基準（2026-06-28、新因子導入前）は下記の表に置き換え。数値の推移が知りたい場合はgit logで旧CLAUDE.mdを参照**

### 馬券種別ROI（n=582レース）

| 馬券種 | 点数/R | 的中率 | 全体ROI | OP | 3勝 | 2勝 |
|---|---|---|---|---|---|---|
| 単勝 | 1 | 19.1% | 64.2% | 54.2% | 75.7% | 59.9% |
| 複勝 | 1 | 43.0% | 75.8% | 64.7% | 79.9% | 81.4% |
| 馬連1-2 | 1 | 8.6% | 133.4% | **215.7%** | **256.5%** | 52.5% |
| 馬連3点BOX | 3 | 15.1% | 82.6% | 129.0% | 103.2% | 47.7% |
| ワイド1-2 | 1 | 18.0% | 126.7% | 157.0% | 108.4% | 136.4% |
| ワイド3点BOX | 3 | 31.8% | 90.0% | 110.8% | 73.0% | 89.0% |
| 三連複1-2-3 | 1 | 4.0% | 162.1% | 403.4%（n少・参考） | 67.9% | 97.9% |
| 三連複1軸4頭 | 6 | 12.9% | 78.6% | 95.4% | **91.8%** | 76.7% |
| 三連複5頭BOX | 10 | 18.4% | 79.9% | **144.3%** | 80.5% | 61.1% |

- 重賞: 全馬券種ROI 41〜204%とバラつくが単勝以外は的中率が低くn小のため引き続き見送り確定維持
- 三連複1-2-3のOP403%は的中率5.7%・n=105で高配当依存の可能性が高く参考値扱い

### 実装済み変更（2026-07-12）
- **jockey_form**: 騎手の通算複勝率ボーナス（30/40%閾値、+1.0/+2.0、3勝クラスは常に+0除外）
- **training_form**: 調教コメント文言別の実績複勝率ベーススコア（A/B/C/D評価を置き換え、全クラス適用）
- **cache_get_before() 修正**: max_days_backデフォルト90→730日（休み明け馬の過去走取りこぼしバグ修正）
- 詳細は本ファイル下部「騎手フォームボーナス」「調教評価スコア」の各セクション参照

### 実運用上の買い方（2026-07-12更新・芝ダート別）

| クラス×コース | 馬連ROI | ワイドROI | 三連複ROI | 買い目 |
|---|---|---|---|---|
| **OP×芝** | 127.2% | 184.4% | 三連複5頭BOX105.3% | 馬連 + ワイド + 三連複5頭BOX |
| **OP×ダ** | 320.8% | 124.4% | 三連複5頭BOX190.6% | 馬連 + ワイド + 三連複5頭BOX |
| **3勝×芝** | 238.3% | 94.0%（赤字） | **三連複1軸4頭117.5%** | 馬連 + 三連複1軸4頭（6点） |
| **3勝×ダ** | 277.3% | 124.8% | 三連複4頭BOX107.4% | 馬連 + ワイド |
| 2勝クラス | 50.9〜54.1% | 51.0〜225.6%（コース差大） | — | 見送り（ダートのワイドのみ突出、要継続観察） |
| 重賞 | 32.8〜102.7% | 78.8〜239.1%（n小） | — | 見送り |

**⚠️ 2026-07-12の重要な修正:** 3勝×芝の三連複は、騎手フォーム・調教評価スコア導入により**純粋4頭BOXのROIが11.5%まで崩壊**（旧466%表記は検証ミスだった旧数値で、現行ロジックでは通用しない）。同じ4頭プールでも**軸1頭+相手4頭（6点・1軸4頭型）に組み替えることでROI117.5%まで回復**することを確認し、`scorer_turf.py`のBOX構成ロジックを置き換えた。3勝×ダは純粋4頭BOX(107.4%)のまま変更なし。

weekend_predict.py 実装内容（2026-07-12更新）:
- OP（box5）: 馬連 + ワイド + 三連複5頭BOX を全面表示（芝ダート問わず）
- 3勝×ダ（box4）: 馬連 + ワイド + 三連複4頭BOX（純粋箱）を表示
- **3勝×芝（box4axis・新設）**: 馬連 + 三連複1軸4頭（6点）を表示。ワイドは引き続き赤字のため非表示

### 三連複「1軸-相手」一本化（2026-08-02・box系を全廃）

**背景:** 2026年7月の実運用で買いサイン13件が全馬券0%（9連敗＋）だった一方、見送りにしたレースの予想1位複勝率は44%で正常。問題は採点ではなく「買いサイン選定」にあった。
夏データ（2024+2025・6場）取得後の再検証で以下が判明:

- **予想順位別の複勝率は単調**（全体: 1位49% > 2位42% > 3位35% > 4位29% > 5位26%…）。予想1位が最も信頼できる軸。
- **取りこぼす3頭目（2頭カバー時）は約51%が予想6〜8位・60%が1〜6番人気**（=top5のすぐ外の人気馬）。
- **ボックス拡張は罠**: 5→6→7頭BOXで的中率は20→28→38%に上がるがROIは106→87→70%に崩壊。
- **1軸-相手2〜6位(10点)が5頭BOXの完全上位互換**（全期間n=688: 的中18.5%/ROI115.4% vs 5box 17.9%/108.8%）。2軸型は不安定な予想2位を必須にするため夏に弱い。

**実装:** クラス別の box4/box4axis/box5 分岐を全廃し、全クラス「1軸(予想1位)-相手」の三連複に統一。
- 相手幅は **`trio_formation.py` の `TRIO_RELAY`** で一元管理（デフォルト相手2〜6位=10点）。クラス別に上書き可能で、ここを編集するだけで weekend_predict.py / scorer_turf.py / scorer_dart.py 全体に反映される（柔軟対応のための唯一のパラメータ）。
- `calc_buy_sign` の sign_level は `box4/box4axis/box5` → **`trio_axis`** に統一。三連単A+B(tierce・3勝×2-5倍)と見送り(skip)は不変。
- CSVは「■三連複1軸-相手」を主買い目、「■参考:2軸型」を補助として出力。
- 検証: 機能導入前検証ルール（n≥50・ROI>100%）はクリア（n=688・ROI115%）。的中率15〜18%で連敗は長引きうる点は許容。

### クラス実績スコア（class_achievement）実装（2026-08-22）

**背景:** 予想1位が市場1番人気より複勝率で通年-13pt劣る（我々49.5% vs 市場63.1%）ことが判明。原因は採点が「勝利数・近走の勢い」偏重で「対戦相手の格(クラス)」を軽視し、格上帰りの実力馬（例: スピードリッチ=1番人気を予想10位に格下げ）を過小評価していたこと。

**発見（キャリブレーション・cache全件）:**
- 単純な「クラス経験の有無」は市場に織り込み済みで独立シグナルなし（人気を揃えると消滅＝4指数却下と同じ罠）。
- 効くのは「格上での通用度（着差）」。ただし**現在クラスより上のクラスでの実績のみ**有効。2勝戦で「OP実績どまり」は複勝21%とベース23%以下（＝同格・格下実績は無価値〜逆効果）、「格上(G1-G3)経験」のみ30%。
- 当初の絶対クラステーブルはOPを悪化させた（OP戦で同格のOP実績を加点し半数の馬を一律底上げ→既存grade因子の解像度を潰した）。**現クラス超えのみ評価する相対化で解決**、OPも改善に転じた。

**実装（`class_achievement.py`・両scorerに統合・既定ON、環境変数 CLASS_ACH=0 で従来に戻せる）:**
- 格×着差テーブルで加点（近5走の「現クラスより上」の最高実績セル。着外=0）。既存の grade_history / prev_high_grade_close / prev2_high_grade_close を置換（二重加点回避）。
- 配点は grade_table_calib.py のキャリブレーション由来（G1/G2/G3/OP × 1着/0.2/0.5/掲示板/着外）。

**A/Bバックテスト（OFF→REL・n=1016）:** 予想1位複勝率 42.9→**44.7%**、単勝ROI 86.8→93.8%、5box 85.7→86.4%。クラス別で単勝は 2勝79→85・3勝101→117・OP95→**103%** と全クラス改善。全クラス適用で確定（OP除外は不要）。OP三連複ROIのみ払戻ブレで微減だが的中率は維持。

### 勝利数ボーナスの「善戦加点」拡張＝❌実装見送り（2026-08-22）

**背景:** 勝利数ボーナス(win_count・最大の加点源+2.5/+5.0)が「1着回数」のみで、勝ちきれないが善戦している強い馬を過小評価している疑い（スピードリッチが1番人気なのに予想10位の一因）。

**分析（穴の存在は事実・cache全件 n=10206）:**
- 直近5走「0勝でも2-3着2回」は複勝**28.1%**で1勝馬(23.3%)を上回るのに加点ゼロ。「2-3着1回」も17.6%だがゼロ。着外のみ0勝(9.2%)との二極化を現行は無視。
- place_consistencyは「3着内3回以上」発火なので、善戦1-2回の馬は両因子の隙間に落ちる（二重加点にはならない）。
- 勝ちクラスの違いはほぼ効かない（現クラス以上勝ち27.7% vs 格下のみ勝ち26.6%）。

**A/B検証で見送り（`check_win_count`に善戦加点を実装したが `WIN_ACH=0` 既定OFFに）:**
- 善戦加点ONで**的中率は微増**（三連複1軸4頭 132→135的中・予想1位複勝率44.7→45.4%）が、**三連複ROIは約-9pt低下**（117.6→108.5%）。
- 外れ値（高配当上位1-3件）を除いても差は残り（ex-top3で94.6% vs 87.1%）、**ノイズでなく構造的低下**。最大配当トップ2は両版とも的中済み＝まぐれの剥落ではない。
- 機序: 善戦加点は人気寄り・堅実馬を持ち上げ「当てる力↑・稼ぐ力(妙味)↓」。主力が妙味依存の三連複・馬連のため、的中+3回のためにROIを削るのは割に合わず**見送り**。
- コードは残置（`WIN_ACH=1`で再検証可能）。的中率・連敗耐性を最優先する方針に転じるなら再検討の余地あり。

### 条件適性4指数プロジェクト（2026-07-18開始・aptitude_index.py）

外部指示書に基づき競馬場巧者・季節・コース属性・開催時期の4指数を1つずつ検証中。
検証期間: 2025/07 + 2025/10〜2026/06（n=630R・2勝以上）。各指数はFEATURE_FLAGSで個別ON/OFF。

- **競馬場巧者指数: ❌却下（2026-07-19）** — キャリブレーションでは強シグナル（場複勝75%+の馬は3着内38.3% vs ベース21.8%）だが、4配点案すべてで馬連1-2・三連複1-2-3のROIがベースライン比悪化。市場と既存項目（複勝安定・同コース実績）に織り込み済みのため。フラグFalse固定・コードは記録として残置
- **季節指数: ❌キャリブレーション段階で棄却（2026-07-19）** — 絶対複勝率では見かけのシグナルがあるが、馬自身の他季節成績との乖離方式で分離すると予測力消滅（非単調・ノイズ）。見かけの正体は近走の好調さ＝複勝安定ボーナスの再測定だった。分析: `analyze/season_deviation_calibration.py`
- **コース属性適性: ❌キャリブレーション段階で棄却（2026-07-19）** — 回りサイズ・直線長さ・起伏・芝種の4次元とも乖離方式で単調シグナルなし。分析: `analyze/course_attr_meet_phase_calibration.py`
- **開催時期補正: ❌キャリブレーション段階で棄却（2026-07-19）** — 「開幕週=前残り」仮説はデータと矛盾（逃げ馬は開幕週-2.4ptで最悪、中盤+8.4ptで最良）。先行有利は全フェーズ共通（+4〜5pt）で開催週に依存せず、既存のpace_fit等と重複
- **共通の根本原因**: 馬の過去走データが直近5走のみのため、指数が「近走の好調さ」の再測定になる。通算の場別・距離別成績（競馬新聞に載っており市場は織り込み済みの情報）を取得しない限り、この系統の指数は独立シグナルにならない

### 未実装・要継続観察
- 2勝クラス×ダートのワイド1-2 ROI225.6%（n=114・的中24件）: 見送りクラス全体の解禁は時期尚早、単勝・複勝など他指標は依然弱い
- OP×芝の三連複1-2-3 ROI403.4%: n=105・的中率5.7%のため高配当1件依存の可能性→継続観察

---

## 騎手フォームボーナス（jockey_form）実装（2026-07-11）

**新規因子:** 騎手の通算複勝率（cache/race_result全件・20騎乗未満はサンプル不足で+0）に基づく加点。
`jockey_form.py` に実装。`scorer_turf.py` / `scorer_dart.py` 双方の `ScoreBreakdown` に追加済み。

**配点:** 複勝率30%未満+0.0 / 30〜40%+1.0 / 40%以上+2.0

**検証結果（n=660、cache/race_result全件・2勝クラス以上）:**
- OP: 三連複5頭BOX ROI 86.0%→131.0%（的中16→21件）、ワイド3点BOX 98.6%→103.2%、三連複4頭BOX 101.8%→120.2%
- 2勝クラス・重賞: 複勝率・ROIともに概ね改善（見送り確定クラスのため実運用への影響なし）
- **3勝クラスのみ一貫して悪化**（馬連1-2 255.5%→205.2%、三連複4頭BOX 59.8%→30-36%、ワイド5頭BOX 101.7%→82.3%で黒字→赤字）
  → 原因: 3勝クラスは既存ロジック単体の単勝的中率が全クラス中最高（23.0%）で、追加因子の伸びしろがすでに使い切られている
  → **`jockey_form.py` の `get_jockey_form_bonus()` は `race_class == 3`（3勝クラス）で常に+0.0を返す設計**
- 検証詳細: `data/騎手ボーナス順位分布レポート_20260711.html`

**干渉確認:** 既存の `jockey_bonus`（重賞×指定7騎手限定）とはスコープが重ならず二重カウントなし。

---

## 調教評価スコア（training_form）実装（2026-07-12）

**変更内容:** `training_rank` の計算方法を、netkeiba A/B/C/D評価の一律点数から**調教コメント文言別の実績複勝率ベース**に変更。
`training_form.py` に実装。`scorer_turf.py` / `scorer_dart.py` 双方の `score_all()` 内 `t_score` 計算を置き換え。

**きっかけ:** 「加点ゼロで3着以内の馬」を調べる過程で、B評価内だけでも実際の複勝率が
7.7%〜43.3%まで開いていることが判明（例: 「更に上昇」43.3% vs 「出来は良」15.2% vs 「手応勝る」7.7%、
いずれもB評価で同じ+2点扱いだった）。A/B/C/Dの4段階が情報を大きく捨てていたための改修。

**配点（2026-07-12 配点スイープで確定）:**
コメントの実績複勝率が 35%以上→+3 / 25〜35%→+2 / 15〜25%→+1 / 8〜15%→0 / 8%未満→-1。
該当コメントの母数が30件未満の場合は従来のA/B/C/D評価（RANK_SCORE）にフォールバック。

**検証結果（n=660、cache/race_result全件・2勝クラス以上）:**
- 3勝クラス: 馬連1-2 255.5%→**256.5%**（的中16→17件）、ワイド1-2 101.1%→**104.5%**、三連複5頭BOX 64.9%→**75.8%**
- OP: ワイド1-2 131.3%→**149.0%**、馬連1-2 215.4%→215.7%（ほぼ横ばい）、三連複5頭BOX 131.0%→119.7%（高水準維持のまま微減）
- jockey_formと異なり**3勝クラスも悪化しないため全クラス共通で適用**（除外ロジックなし）

**データ品質の副次的な発見・対応（いずれも2026-07-12に修正済み）:**
- `cache_get_before()` のデフォルト `max_days_back=90` が休み明け90日超の馬の過去走を取りこぼす既知バグを発見・修正
  → デフォルトを**730日（2年）**に変更（scorer側の2年縛りと統一）。修正後の取得成功率は99.7%→99.92%
  → 残る0.08%（7頭）はスナップショット自体が存在しない真の欠損で、max_days_backでは直せない（要スクレイピング・未対応）
- `cache/netkeiba_training/` に2026年6月21日の対象クラス9レース分の欠損を発見（`fetch_training_cache.py`の再実行漏れ）→ `fetch_training_cache.py` を再実行し補完済み（2129/2129件に復旧）
- **運用ルール:** race_result キャッシュに新しい日付分を追加したら、必ず `fetch_training_cache.py` を再実行して調教データの欠損がないようにすること

---

## オッズ取得ルール（2026-06-27更新）

### 状況別の取得元（必ず区別すること）

| 状況 | データソース | キー/関数 |
|---|---|---|
| **バックテスト（過去データ）** | `cache/race_result/*.json` の `entries[].odds` / `entries[].popularity` | キャッシュ直読み・外部取得禁止 |
| **当日予想 オッズ（第1優先）** | JRA公式出馬表 `pw01dde01...` → `HorseEntry.odds` / `HorseEntry.popularity` | `_fetch_jra_odds()` |
| **当日予想 オッズ（フォールバック）** | netkeiba `shutuba.html` → `Shutuba_Table` | `fetch_odds()` |
| **当日予想 馬場状態** | JRA公式出馬表 `accessD.html` → `parse_race_info().track_condition` | `_fetch_jra_odds()` の戻り値に含まれる |

### バックテスト時のオッズ使い方（外部アクセス不要）

```python
for e in data["entries"]:
    odds = e.get("odds", 0)        # 単勝オッズ
    pop  = e.get("popularity", 99) # 人気
```

### 当日予想のオッズ・馬場状態取得フロー（weekend_predict.py）

```python
jra_odds_map, jra_track = _fetch_jra_odds(race_id, race_date, entries)
if jra_odds_map:
    odds_map = jra_odds_map          # JRA優先
else:
    odds_raw = fetch_odds(race_id)   # netkeibaフォールバック
```

- **馬場状態は出馬表（accessD.html）から取得**: `parse_race_info().track_condition` に当日の最新値が入っている
- **`/keiba/baba/` は使わない**: このページは「前日正午現在」の古い情報であり、当日の実際の馬場状態と異なる場合がある
- **発走前**: JRA公式出馬表からオッズ・馬場状態を同時取得できる
- **発走後（当日中）**: オッズは `0.0`（発走後は数値なし）→ 正常挙動
- netkeiba `odds_get_form.html` は全オッズが `---.-` (JSプレースホルダー) → 実質使えない

### JRA URL生成（jra_scraper.py）の注意点

チェックサム計算式（2026-06-27更新・全18データポイントで検証済み）:
```python
race_contrib = ((race - 1) * 181 + (64 if race >= 10 else 0)) % 256
base = (20 + venue*157 + kai*210 + nichi*48) % 256
checksum = (base + race_contrib) % 256
```
- race 1-9: (race-1) * 181（mod 256）
- race 10-12: さらに+64オフセット
- venue係数: 157（旧10は廃止）、kai係数: 210（旧84は廃止）、nichi係数: 48（不変）
- JRA出馬表は**発走前のみ**アクセス可能。発走後はパラメータエラーになる（正常挙動）

---

## 事前確認エージェントルール（2026-06-20制定）

**予想・バックテスト結果を出力する前に、以下を必ず自己チェックする:**

### 予想時チェックリスト

1. **オッズ取得確認**: `odds_map` が空でないか
   - `_fetch_jra_odds()` (JRA公式) が第1優先 → 発走前のみ有効
   - フォールバック: `fetch_odds()` (netkeiba) → 実質`---.-`で機能しないことが多い
   - 発走後レースのオッズ未取得は正常。⚠警告を出してスキップ
2. **馬場状態取得確認**: `jra_track` が取れているか → `_fetch_jra_odds()` の第2戻り値
   - 稍重/重/不良なら道悪実績スコアが加算される → 馬場確認は必須
3. **クラス判定確認**: `race_class` が正しく判定されているか（OPと3勝とGIIIの誤判定に注意）
4. **見送りルール適用確認**: 18頭/乖離3〜5pt/2勝以下/重賞が正しく除外されているか
5. **馬名マッチング確認**: netkeiba馬名とオッズマップの馬名が一致しているか

### バックテスト時チェックリスト

1. **オッズ取得元の確認**: バックテストは必ず `cache/race_result/*.json` の `entries[].odds` を使う
   - JRA公式・netkeibaへの外部アクセスは不要・禁止（過去レースは発走後でURL無効のため）
2. **対象期間・クラス・馬場の確認**: コードに明示してから実行
3. **投資額の計算基準**: 1点=100円固定。複数点の場合は `N * pts * 100` で投資額計算
4. **的中判定ロジック**: `frozenset` の比較で馬番一致を確認（馬名ではなく馬番）
5. **払戻データ存在確認**: `cache/payouts/` が存在しないレースはスキップしているか
6. **クラス分類の表示**: 全体・クラス別・芝/ダート別を必ず出力

### コードミスが多いパターン（要注意）

- `'3連複'` vs `'三連複'` → 正しいキーは **`'3連複'`**
- 馬番パース `_pn_for_count()` を使わず greedy パースすると誤読 → 必ず件数を渡す
- `cache_get_before()` の `max_days_back` デフォルトは730日（2026-07-12修正済み、旧90日は既知バグだったため変更）
- 芝/ダート混在でコメントする際は「芝のみ実運用対象」を前提にすること

---

## 見送りロジック バックテスト結果・改定（2026-06-16）

**バックテスト対象:** 401R（in-memory採点・2勝クラス以上）

**廃止した見送りルール:**
- 断然人気（軸馬オッズ<2倍）の見送り → **廃止**（5BOX回収率141%・複勝率87.9%で明確に黒字）
- 乖離≥5pt 高信頼7点推奨 → **廃止**（単勝ROI50%・5BOX ROI40%でバックテスト不合格）

**維持した見送りルール（バックテストで正当化）:**
- 18頭フルゲート: 複勝率25.8% / 複勝ROI48% → 見送り維持
- 乖離3〜5pt: 5BOX ROI69% → 見送り維持
- 2勝クラス以下: 単勝ROI79% / 5BOX ROI73% → 見送り維持
- 重賞（GI/GII/GIII）: 三連複ROI6%以下・n=92 → 見送り確定
